ENTITY:

- dont store strings in instances, store in map, save index in struct
- dont take registry raw pointer, take reference and then implement copy/move

Managers:

- re-createto class

Resource:
- think about int ids or hashes or hashes

General:
- create tilemap component
- craete tilemap system

Animation:
- rethink how we add animation
- think of it from how we'd do it from an editor
- we specify amount of frames and then tie a sprite to a frame
